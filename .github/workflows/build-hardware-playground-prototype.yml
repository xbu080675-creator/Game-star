name: Build Hardware Playground Prototype

on:
  push:
    branches: [ feature/shoulder-ignition-boot ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      GSB_DEV_KEYSTORE_B64: ${{ secrets.GSB_DEV_KEYSTORE_B64 }}
      GSB_DEV_STORE_PASSWORD: ${{ secrets.GSB_DEV_STORE_PASSWORD }}
      GSB_DEV_KEY_PASSWORD: ${{ secrets.GSB_DEV_KEY_PASSWORD }}
      GSB_DEV_KEY_ALIAS: ${{ secrets.GSB_DEV_KEY_ALIAS }}
      ANDROID_HOME: /usr/local/lib/android/sdk
      ANDROID_SDK_ROOT: /usr/local/lib/android/sdk

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Use runner Android SDK
        shell: bash
        run: |
          set -euo pipefail
          SDKMANAGER="$(find "$ANDROID_SDK_ROOT/cmdline-tools" -path '*/bin/sdkmanager' -type f | sort -V | tail -1)"
          test -x "$SDKMANAGER"
          yes | "$SDKMANAGER" --licenses >/dev/null || true
          "$SDKMANAGER" "platform-tools" "platforms;android-35" "build-tools;35.0.0"

      - name: Set up Gradle 8.9
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Restore fixed DEV signing identity
        shell: bash
        run: |
          set -euo pipefail
          test -n "$GSB_DEV_KEYSTORE_B64"
          test -n "$GSB_DEV_STORE_PASSWORD"
          test -n "$GSB_DEV_KEY_PASSWORD"
          test -n "$GSB_DEV_KEY_ALIAS"
          printf '%s' "$GSB_DEV_KEYSTORE_B64" | tr -d '\r\n\t ' | base64 -d > "$RUNNER_TEMP/gsb-dev.keystore"
          keytool -list -keystore "$RUNNER_TEMP/gsb-dev.keystore" -storepass "$GSB_DEV_STORE_PASSWORD" -alias "$GSB_DEV_KEY_ALIAS" >/dev/null
          echo "GSB_DEV_KEYSTORE=$RUNNER_TEMP/gsb-dev.keystore" >> "$GITHUB_ENV"

      - name: Fetch fixed CC0 menu music
        shell: bash
        run: |
          set -euo pipefail
          MUSIC_URL='https://opengameart.org/sites/default/files/spacelifeNo14.ogg'
          MUSIC_SHA256='64ba0b12ee09c106bca11531010534df282f3474a88fb1923d55b3de469898d4'
          MUSIC_OUT='app/src/main/assets/menu_bgm.ogg'
          mkdir -p app/src/main/assets
          curl -fL --retry 4 --retry-delay 2 --retry-all-errors "$MUSIC_URL" -o "$MUSIC_OUT"
          echo "$MUSIC_SHA256  $MUSIC_OUT" | sha256sum -c -

      - name: Guard hardware playground contract
        shell: bash
        run: |
          set -euo pipefail
          BOOT='app/src/main/assets/shoulder-boot.html'
          ACTIVITY='app/src/main/java/com/xbu/esportscenter/BootMainActivity.java'
          MOTOR='app/src/main/java/com/xbu/esportscenter/platform/android/HardwareAwakeningMotorDriver.java'
          MOTION='app/src/main/java/com/xbu/esportscenter/platform/android/HardwarePlaygroundMotionAdapter.java'
          PLAY_HAPTIC='app/src/main/java/com/xbu/esportscenter/platform/android/HardwarePlaygroundHapticAdapter.java'
          BOOTSTRAP='app/src/main/java/com/xbu/esportscenter/privileged/RedMagicShoulderBootstrapProvider.java'
          MANIFEST='app/src/main/AndroidManifest.xml'

          grep -q 'HARDWARE PLAYGROUND' "$BOOT"
          grep -q 'touchCanvas' "$BOOT"
          grep -q 'hapticPad' "$BOOT"
          grep -q 'fanDrag' "$BOOT"
          grep -q 'THERMAL FAN MODEL' "$BOOT"
          grep -q '0815 LINEAR MOTOR' "$BOOT"
          grep -q 'onGSBMotion' "$BOOT"
          grep -q 'GSBPlayground' "$ACTIVITY"

          grep -q 'SensorManager' "$MOTION"
          grep -q 'TYPE_GAME_ROTATION_VECTOR' "$MOTION"
          grep -q '\[GSB-PLAYGROUND\]' "$MOTION"
          grep -q 'HardwarePlaygroundMotionAdapter' "$ACTIVITY"

          grep -q 'VibratorManager' "$PLAY_HAPTIC"
          grep -q 'USAGE_HARDWARE_FEEDBACK' "$PLAY_HAPTIC"
          grep -q '\[GSB-PLAY-HAPTIC\]' "$PLAY_HAPTIC"
          grep -q 'playSample' "$PLAY_HAPTIC"
          grep -q 'HardwarePlaygroundHapticAdapter' "$ACTIVITY"

          grep -q 'hardware_playground_migration_0427_v5' "$BOOTSTRAP"
          grep -q 'android.permission.VIBRATE' "$MANIFEST"
          grep -q 'VibratorManager' "$MOTOR"
          grep -q 'USAGE_HARDWARE_FEEDBACK' "$MOTOR"

          if grep -Eq 'startHum\(|humGain|frequency\.value=4[0-9]|tone\((58|74|96),' "$BOOT"; then
            echo 'Speaker fake-haptic path detected' >&2
            exit 1
          fi

      - name: Compile signed playground APK
        run: gradle :app:assembleRelease --stacktrace

      - name: Stage artifact
        shell: bash
        run: |
          set -euo pipefail
          cp app/build/outputs/apk/release/app-release.apk Game-Star-Box-0.4.27-hardware-playground-prototype.apk
          sha256sum Game-Star-Box-0.4.27-hardware-playground-prototype.apk

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Game-Star-Box-0.4.27-hardware-playground-prototype
          path: Game-Star-Box-0.4.27-hardware-playground-prototype.apk
          if-no-files-found: error
          retention-days: 3
